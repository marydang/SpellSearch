-- Unit tests for parsing in the WhatGives addon.
-- NOTE: must be run WITHIN the test/ directory, i.e:
--   lua test_SpellSearch.lua

require("TestBase")

-- Create upvalues
local _U = FitzUtils

-- Tests for parsing in theb SpellSearch addon.
TestAddon = {}
    function TestAddon:setUp()
       -- Set up for test.  This is run prior to each test executing.
    end

    function TestAddon:test_Basic()
       -- Basic unit test.  Obviously does nothing.
       local pass = true
       assertEquals(pass, true)
    end


-- Run all tests unless overriden on the command line.
LuaUnit:run("TestAddon")
